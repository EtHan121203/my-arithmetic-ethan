stages:
  - build
  - test
  - coverage

test:
  stage: test
  image: python:3.12
  script:
    - pip install hatch
    - hatch test

coverage:
  stage: coverage
  image: python:3.12
  script:
    - pip install hatch
    - hatch test --cover
  coverage: '/TOTAL.+?(\d+\%)$/'
  only:
    - main

build_stable:
  stage: build
  image: python:3.12
  script:
    - echo "Déploiement my-arithmetic-$USER sur les serveurs stables"
    - pip install hatch hatch-vcs
    - hatch build
    - ls dist/
  only:
    - tags

build_non_stable:
  stage: build
  image: python:3.12
  script:
    - echo "Déploiement my-arithmetic-$USER sur les serveurs develop"
    - pip install hatch hatch-vcs
    - hatch build
    - ls dist/
  only:
    - develop

sync-with-github:
  before_script:
    - apk add --no-cache git
    - git config --global user.name "${GITLAB_USER_NAME}"
    - git config --global user.email "${GITLAB_USER_EMAIL}"
    - git remote remove github || true
    - git remote add github https://oauth2:${ACCESS_TOKEN}@github.com/${CI_PROJECT_NAMESPACE}/${CI_PROJECT_NAME}.git
    - |
      # Vérifier si la branche main existe, sinon utiliser master
      if git ls-remote --exit-code github main; then
        BRANCH="main"
      else
        BRANCH="master"
      fi
  script:
    - git checkout $BRANCH
    - git pull origin $BRANCH
    - git pull github $BRANCH
    - git push origin HEAD:$BRANCH
